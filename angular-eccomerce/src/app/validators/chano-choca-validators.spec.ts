import { ChanoChocaValidators } from './chano-choca-validators';

describe('ChanoChocaValidators', () => {
  it('should create an instance', () => {
    expect(new ChanoChocaValidators()).toBeTruthy();
  });
});
